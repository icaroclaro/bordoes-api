file: #para salvar aquivo
  upload-dir: /Users/4960022981/Desktop/arquivos-api-upload/
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/bordoes?useTimezone=true&serverTimezone=UTC
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true
  servelet: #para salvar arquivos
    multipart:
      enabled: true
      file-size-threshold: 2KB
      max-file-size: 50MB
      max-request0size: 60MB
server:
  error:
    include-stacktrace: never
